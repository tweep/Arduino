This directory contains a library which uses the Neopixel library. 

The libary itself implements different color transitions for LED strands. 
- rainbow transition 
- hsv transition ( green -> yellow -> red ) 
- transition on a limited spectrum band - ie. from  blue - purple and back 
